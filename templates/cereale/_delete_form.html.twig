{% block body %}
<form method="post" action="{{ path('cereale_delete', {'id': cereale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cereale.id) }}">
    <button class="btn">Delete</button>
</form>
{% endblock %}