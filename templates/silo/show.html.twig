{% extends 'template.html.twig' %}

{% block title %}Silo{% endblock %}

{% block body %}
    <h1>Silo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ silo.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ silo.Nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ silo.Adresse }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ silo.Capacite }}</td>
            </tr>
            <tr>
                <th>CodeS</th>
                <td>{{ silo.CodeS }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('silo_index') }}">back to list</a>

    <a href="{{ path('silo_edit', {'id': silo.id}) }}">edit</a>

    {{ include('silo/_delete_form.html.twig') }}
{% endblock %}
